{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd transpolink-api && npm install && npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "cd transpolink-api && npx prisma migrate deploy && node dist/main",
    "healthcheckPath": "/api/v1/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
